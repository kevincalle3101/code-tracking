# Perfil de Desarrollo
NODE_ENV: dev

# PostgreSQL
db:
  host: localhost
  port: 5432
  username: postgres
  password: "mysecretpassword"
  database: dev_db

# AWS
aws:
  region: us-east-1
  accessKeyId: "TU_ACCESS_KEY_DEV"
  secretAccessKey: "TU_SECRET_KEY_DEV"

# Puerto de la aplicación
PORT: 3000